/*
Pure Virtual Functions: Define an abstract class Media with a pure virtual function play(). Create derived classes Audio and Video that provide their own implementations of play(). Show how you can use a pointer to Media to handle different types of objects.
*/